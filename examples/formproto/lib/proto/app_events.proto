syntax = "proto3";

package hyttahub.example.formproto;

message AppEvent {
  oneof event {
    UpdateText update_text = 1;
    UpdateSubmitted update_submitted = 2;
  }
}

message UpdateText {
  string text = 1;
}

message UpdateSubmitted {
  bool submitted = 1;
}
