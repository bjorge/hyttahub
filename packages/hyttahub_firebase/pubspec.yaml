name: hyttahub_firebase
description: "Firebase implementations for hyttahub base functionality."
repository: https://github.com/bjorge/hyttahub
version: 0.1.2
# publish_to: none

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  hyttahub: ^0.1.53

  cloud_firestore: ^6.0.2
  firebase_core: ^4.1.1
  firebase_auth: ^6.1.0
  firebase_storage: ^13.0.2
  cloud_functions: ^6.0.2
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
